import React, { Component } from 'react';

const RightCounter = props => (
    <div className = 'fixed bottom-0 w-25 h-25 items-center justify-center bottom-0-l right-0 bg-blue'>
        RightCounter section
    </div>
)

export default RightCounter;