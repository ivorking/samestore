import React, { Component } from 'react';

const RightCounter = props => (
    <div className = 'bottom-0 fixed w-25 h-25 inline-flex items-center justify-center bottom-0-l right-0 bg-blue'>
        RightCounter section
    </div>
)

export default RightCounter;